apiVersion: v1
kind: Service
metadata:
  name: mysql
  namespace: group-wordpress
spec:
  selector:
    app: mysql
  ports:
    - port: 3306
      targetPort: 3306
  clusterIP: None   # headless service is fine for a single DB pod; use a normal ClusterIP if your grading rubric requires one